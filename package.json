{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node scripts/check-package-manager.cjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck && tsc -p api/tsconfig.json",
    "build:vercel": "pnpm run typecheck && pnpm --filter @workspace/sentriq-demo run build",
    "dev": "pnpm --filter @workspace/sentriq-demo run dev",
    "test": "pnpm --filter @workspace/sentriq-demo run test"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3",
    "@types/node": "catalog:"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": "24.x"
  }
}
